{% extends 'AppBundle::base.html.twig' %}

{% block title parent() ~ ' - Articles' %}

{% block nav %}
    <h2>{{ 'article-categories.title'|trans }}</h2>
    <ol class="breadcrumb">
        <li>
            <a href="{{ path('home') }}">{{ 'home.title'|trans }}</a>
        </li>
        <li class="active">
            <strong>{{ 'articles-categories.title'|trans }}</strong>
        </li>
    </ol>
{% endblock %}

{% block action %}
    <a href="{{ path('post_categories') }}" class="btn btn-primary">{{ 'article-categories.action.add'|trans }}</a>
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-title">
                    <h5>{{ 'article-categories.list.title'|trans }}</h5>
                </div>
                <div class="ibox-content">
                    <div class="project-list">
                        <table class="table table-hover">
                            <tbody>
                            {% for category in categories %}
                                <tr>
                                    <td class="col-xs-1"># {{ category.id }}</td>
                                    <td class="project-title"><strong>{{ category.label }}</strong></td>
                                    <td>{{ category.articles|length }} {{ 'article-categories.list.msg.nb-articles' }}</td>
                                    <td class="text-right">
                                        <a class="btn btn-danger btn-xs"
                                           href="{{ path('delete_category', {id: category.id}) }}"
                                           {% if category.articles|length > 0 %}disabled{% endif %}>{{ 'article-category.list.action.view'|trans }}</a>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
