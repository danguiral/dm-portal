{% extends 'AppBundle::base.html.twig' %}

{% block title parent() ~ ' - ' ~ 'articles.add.title'|trans %}

{% block nav %}
    <h2>{{ 'articles.add.title'|trans }}</h2>
    <ol class="breadcrumb">
        <li>
            <a href="{{ path('home') }}">{{ 'home.title'|trans }}</a>
        </li>
        <li>
            <a href="{{ path('get_articles') }}">{{ 'articles.title'|trans }}</a>
        </li>
        <li class="active">
            <strong>{{ 'articles.add.title'|trans }}</strong>
        </li>
    </ol>
{% endblock %}

{% block action %}
    <a href="{{ path('get_articles') }}" class="btn btn-default">{{ 'articles.add.action.cancel'|trans }}</a>
{% endblock %}


{% block content %}
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-title">
                    <h5>{{ 'articles.add.title'|trans }}</h5>
                </div>
                <div class="ibox-content">
                    {{ form_start(form) }}
                    <div class="form-group">
                        {{ form_errors(form.title) }}
                        {{ form_widget(form.title, {'attr': {'class': 'form-control', 'placeholder': 'entity.article.title.placeholder'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_errors(form.category) }}
                        {{ form_widget(form.category, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_errors(form.description) }}
                        {{ form_widget(form.description, {'attr': {'class': 'form-control', 'placeholder': 'entity.article.description.placeholder'}}) }}
                    </div>
                    <button type="submit" class="btn btn-primary block full-width m-b">{{ 'articles.add.action.submit'|trans }}</button>
                    {{ form_rest(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
